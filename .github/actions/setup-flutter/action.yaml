name: Setup Flutter + Melos
runs:
  using: composite
  steps:
    - uses: subosito/flutter-action@v2.21.0
      with:
        channel: stable
        version: "3.38.4"
        cache: true
    - run: flutter pub global activate melos
      shell: bash
